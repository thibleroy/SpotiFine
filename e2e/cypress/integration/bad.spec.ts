describe('Initial test suite', () => {
    it('should get code coverage', () => {
        cy.visit('https://localhost:8100');
    });
    it('should get code coverage', () => {
        cy.visit('https://localhost:8100');
    });
});
